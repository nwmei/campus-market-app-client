query StoreItem($input: StoreItemInput!) {
    storeItem(input: $input) {
        id,
        name,
        description,
        imageUrl,
        category,
        neighborhood,
        price,
        seller {
            id,
            firstName,
            lastName,
            emailAddress
        },
        comments {
            commenterFullName,
            commentText,
            commenterImageUrl
        },
        likes,
        date
    }
}
query SessionUserDetails($input: SessionUserDetailsInput!) {
  sessionUserDetails(input: $input) {
    id,
    firstName,
    lastName,
    emailAddress,
    imageUrl,
    likedItems,
    listedItems
  }
}
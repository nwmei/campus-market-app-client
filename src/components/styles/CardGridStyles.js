import {makeStyles} from "@material-ui/core/styles";

const useStyles = makeStyles({
    gridContainer: {
        paddingLeft: "0px",
        paddingRight: "40px",
    }
});

export default useStyles;
import {makeStyles} from "@material-ui/core/styles";

const useStyles = makeStyles({
    gridContainer: {
        paddingLeft: "0px",
        paddingRight: "40px",
        paddingTop: "10px",
    }
});

export default useStyles;
import {makeStyles} from "@material-ui/core/styles";

const useStyles = makeStyles({
    gridContainer: {
        paddingLeft: "0px",
        paddingRight: "0px",
        paddingTop: "10px",
    },
    buttonsMobile: {
        paddingRight:20
    }
});

export default useStyles;